/* $(document).ready(() => {
	$(window).scroll(() => {
		if ($(window).scrollTop() < 100) {
			$('.home .site-header__wrapper').css(
				'background',
				'linear-gradient(180deg, #000000 0%, rgba(0, 0, 0, 10%) 100%)',
			);
		} else {
			$('.home .site-header__wrapper').css(
				'background',
				'url("../images/wavy-top-nav.svg") no-repeat center center/cover',
			);
		}
	});
}); */
